import React from 'react';
import { GetStaticProps } from 'next';

// Components
import Grid from '@material-ui/core/Grid';
import Typography from '@material-ui/core/Typography';

// Others
import useGlobalStyles from 'src/styles';

export const {{ pascalCase pageName }}Page = ({ pageMeta }: {{ pascalCase pageName }}PageProps) => {
  const globalClasses = useGlobalStyles();

  return (
    <Grid
      container
      direction="row"
      justify="center"
      alignItems="center"
    >
      <Grid item xs={12}>
        <Typography variant="h1">
          {{ pascalCase pageName }}Page
        </Typography>
      </Grid>
    </Grid>
  );
};

export const getStaticProps: GetStaticProps = async () => {
  return {
    props: {
      pageMeta: {
        metaTitle: 'BinaryEdge Tech Task by @andrewizbatista',
        metaDescription: 'BinaryEdge Tech Task by @andrewizbatista',
      },
    },
  };
};

export interface {{ pascalCase pageName }}PageProps {
  pageMeta: PageMeta;
}

export default {{ pascalCase pageName }}Page;
